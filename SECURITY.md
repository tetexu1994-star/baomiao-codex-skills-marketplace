# 安全政策

请不要在公开 Issue 附上 token、Cookie、私有仓库地址或含敏感信息的日志。上线仓库后，请通过 GitHub Security Advisories 私下报告市场管线、条目或客户端契约中的漏洞。

市场不执行候选内容。自动同步只产生候选，含可执行文件、未知许可证、秘密模式、高危命令或高风险权限的内容默认阻断。已发布条目仍不等于绝对安全；客户端必须在每次安装时重新校验固定提交和完整目录。

如果发现已发布条目受影响，维护者应先从 `catalog/approved/` 移除并发布新目录摘要，再调查和恢复；不应静默把来源改到另一个提交。

